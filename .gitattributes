* text=auto

# 컨테이너 안에서 실행되는 스크립트는 반드시 LF 여야 함
# (CRLF 로 커밋되면 리눅스에서 "exec format error" / "no such file" 로 죽음)
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
Dockerfile text eol=lf
Caddyfile text eol=lf
*.sql text eol=lf
