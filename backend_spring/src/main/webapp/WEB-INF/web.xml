<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
         http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">

    <!-- Root ApplicationContext (DB, MyBatis, Service, Util) -->
    <context-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>/WEB-INF/spring/root-context.xml</param-value>
    </context-param>
    <listener>
        <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>

    <!-- DispatcherServlet (Controller, MVC) -->
    <servlet>
        <servlet-name>dispatcher</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <init-param>
            <param-name>contextConfigLocation</param-name>
            <param-value>/WEB-INF/spring/servlet-context.xml</param-value>
        </init-param>
        <load-on-startup>1</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>dispatcher</servlet-name>
        <url-pattern>/</url-pattern>
    </servlet-mapping>

    <!--
        필터 실행 순서: web.xml 에 선언된 순서대로 실행됩니다.
        1. encodingFilter  — 요청/응답 UTF-8 보장
        2. corsFilter      — CORS 헤더 설정 + OPTIONS preflight 처리
        3. jwtFilter       — /api/chat/*, /api/documents/* 에 적용, JWT 검증
    -->

    <!-- 1. UTF-8 인코딩 -->
    <filter>
        <filter-name>encodingFilter</filter-name>
        <filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
        <init-param><param-name>encoding</param-name><param-value>UTF-8</param-value></init-param>
        <init-param><param-name>forceEncoding</param-name><param-value>true</param-value></init-param>
    </filter>
    <filter-mapping>
        <filter-name>encodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- 2. CORS (SimpleCorsFilter) — /* 전체 적용, OPTIONS 를 여기서 차단 -->
    <filter>
        <filter-name>corsFilter</filter-name>
        <filter-class>com.legal.backend.filter.SimpleCorsFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>corsFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- 3. JWT — /api/chat/*, /api/documents/* 에 적용 -->
    <filter>
        <filter-name>jwtFilter</filter-name>
        <filter-class>com.legal.backend.filter.JwtFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>jwtFilter</filter-name>
        <url-pattern>/api/chat/*</url-pattern>
    </filter-mapping>
    <filter-mapping>
        <filter-name>jwtFilter</filter-name>
        <url-pattern>/api/documents/*</url-pattern>
    </filter-mapping>

</web-app>