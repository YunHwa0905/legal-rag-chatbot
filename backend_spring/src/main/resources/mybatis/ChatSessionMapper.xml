<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.legal.backend.dao.ChatSessionDao">

    <select id="findByUser" resultType="ChatSession">
        SELECT id, user_id AS userId, title, created_at AS createdAt,
               updated_at AS updatedAt, deleted_at AS deletedAt
        FROM chat_session
        WHERE user_id = #{userId} AND deleted_at IS NULL
        ORDER BY updated_at DESC
    </select>

    <select id="findById" resultType="ChatSession">
        SELECT id, user_id AS userId, title, created_at AS createdAt,
               updated_at AS updatedAt, deleted_at AS deletedAt
        FROM chat_session
        WHERE id = #{id} AND deleted_at IS NULL
    </select>

    <insert id="insert" parameterType="ChatSession" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_session (user_id, title)
        VALUES (#{userId}, #{title})
    </insert>

    <update id="updateTitle">
        UPDATE chat_session SET title = #{title} WHERE id = #{id}
    </update>

    <!--
        NOW() -> datetime('now','localtime')

        SQLite 의 CURRENT_TIMESTAMP 는 UTC 라, 스키마의 기본값
        datetime('now','localtime') 과 섞이면 같은 테이블 안에서 9시간 어긋난 값이
        생깁니다. 시각을 넣는 모든 자리에서 localtime 으로 통일합니다.
        (localtime 이 KST 가 되려면 프로세스에 TZ=Asia/Seoul 이 필요합니다)
    -->
    <update id="softDelete">
        UPDATE chat_session SET deleted_at = datetime('now','localtime') WHERE id = #{id}
    </update>

    <update id="touch">
        UPDATE chat_session SET updated_at = datetime('now','localtime') WHERE id = #{id}
    </update>

</mapper>
